# accounts/models.py
# Using Django's built-in User model (django.contrib.auth.models.User)
# No custom User model needed for this project.
#
# If you need to extend the User model in the future, use:
# from django.contrib.auth.models import AbstractUser
#
# class User(AbstractUser):
#     # Add custom fields here
#     pass
#
# And set AUTH_USER_MODEL = 'accounts.User' in settings.py
